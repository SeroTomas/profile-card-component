---
interface Props {
  value: string;
  title: string;
}

const { value, title } = Astro.props;
---

<div class="flex flex-col text-center w-1/3">
  <span class="text-lg font-bold">{value}</span>
  <span class="text-xs opacity-65 tracking-wider">{title}</span>
</div>
